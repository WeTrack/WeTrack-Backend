description = 'Client module, can be used by Android client to access the REST services' +
        ' published by `service` module'

dependencies {
    compile "org.glassfish.jersey.core:jersey-client:$jerseyVersion"
}
